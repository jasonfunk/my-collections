# EAS installs dependencies itself — never upload node_modules
node_modules/

# Android Gradle build artifacts (rebuild in the cloud)
android/app/build/
android/.gradle/
android/build/

# Other workspace packages not needed for the mobile build
packages/api/
packages/web/

# Mobile build output and Expo cache
dist/
.expo/

# Dev tooling and docs
.git/
devops/
docs/
postman/
